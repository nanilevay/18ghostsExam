using System;
namespace _18ghostsExam
{
    /// <summary>
    /// this enum allows us to store the 4 directions in which portals can face
    /// </summary>
    public enum PortalDir
    {
        up = 'u',
        down = 'd',
        left = 'l',
        right = 'r'
    }

}