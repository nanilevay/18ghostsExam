using System;
namespace _18ghostsExam
{
    public class RedTiles : Tiles
    {
        public RedTiles(Positions pos)
        {
            /*
            Pos = pos;
            Colour = Colours.red;
            Character = Characters.map;
            */
        }
    }
}
