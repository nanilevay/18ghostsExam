using System;
namespace _18ghostsExam
{
    /// <summary>
    /// This enum allows us to set the characters for the console version of the
    /// game and to render them when necessary
    /// </summary>
    public enum Characters
    {
        // mirror tile
        mirror = 'm',

        // ghost tile
        ghost = 'g',

        // portal tile
        portal = 'p',

        // regular tile
        map = '.'
    }
}
